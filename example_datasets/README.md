# Example Datasets

These datasets are provided as examples, and that's why they are available in the repo.

Large datasets should not be pushed. Use directory `datasets/`, which is include in the `.gitignore`, for storing large datasets locally.
