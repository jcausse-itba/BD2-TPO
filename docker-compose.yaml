services:

  # MongoDB Container
  mongo:
    image: mongo:latest
    container_name: bd2-tpo-g10-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
      - ./init-mongo.sh:/docker-entrypoint-initdb.d/init-mongo.sh:ro
      - ./example_datasets:/example_datasets:ro
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
      - MONGO_INITDB_DATABASE=${MONGO_INITDB_DATABASE}
    healthcheck:
      test: ["CMD-SHELL", "mongosh -u $$MONGO_INITDB_ROOT_USERNAME -p $$MONGO_INITDB_ROOT_PASSWORD --authenticationDatabase admin --eval \"db.adminCommand('ping')\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Apache Cassandra Container
  cassandra:
    image: cassandra:latest
    container_name: bd2-tpo-g10-cassandra
    ports:
      - "9042:9042"
    volumes:
      - cassandra_data:/var/lib/cassandra
    healthcheck:
      test: ["CMD-SHELL", "nodetool status | grep -q '^UN'"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s

  # Apache Cassandra Init Container
  cassandra-init:
    build:
      context: .
      dockerfile: init-cassandra.Dockerfile
    container_name: bd2-tpo-g10-cassandra-init
    volumes:
      - ./example_datasets:/example_datasets:ro
      - ./init-cassandra.sh:/init-cassandra.sh:ro
      - cassandra_data:/cassandra_data
    environment:
      - CASSANDRA_HOST=${CASSANDRA_HOST}
      - CASSANDRA_KEYSPACE=${CASSANDRA_KEYSPACE}
    depends_on:
      cassandra:
        condition: service_healthy
    restart: on-failure

  # Express.js API Container
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: bd2-tpo-g10-api
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=${MONGO_URI}
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
      - MONGO_INITDB_DATABASE=${MONGO_INITDB_DATABASE}
      - CASSANDRA_HOST=${CASSANDRA_HOST}
      - CASSANDRA_DC=${CASSANDRA_DC}
      - CASSANDRA_KEYSPACE=${CASSANDRA_KEYSPACE}
    depends_on:
      mongo:
        condition: service_healthy
      cassandra-init:
        condition: service_completed_successfully

  # NGINX Frontend Container
  frontend:
    image: nginx:alpine
    container_name: bd2-tpo-g10-frontend
    ports:
      - "8080:80"
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api

volumes:
  mongo_data:
    driver: local
    name: bd2-tpo-g10-mongo
  cassandra_data:
    driver: local
    name: bd2-tpo-g10-cassandra
